export enum LandingDataChildTypeEnum {
  Product = "product",
  Collection = "collection",
}

/* export enum LandingDataLinkType {
  Product = "product",
  Collection = "collection",
} */

export enum LandingRowHeight {
  SM = "sm",
  LG = "lg",
  XL = "xl",
}
